# Stage 0 baseline. Revisited in Stage 10 (docs/09-BUILD-PLAN.md) once
# release builds are actually exercised — kotlinx.serialization and Room in
# particular need explicit keep rules that only make sense once real
# @Serializable/@Entity types exist.
